import 'package:flutter/material.dart';

class ShopsProductsPage extends StatelessWidget {
  const ShopsProductsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Products Page',
        style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold),
      ),
    );
  }
}