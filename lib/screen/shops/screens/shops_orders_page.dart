import 'package:flutter/material.dart';

class ShopsOrdersPage extends StatelessWidget {
  const ShopsOrdersPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Orders Page',
        style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold),
      ),
    );
  }
}